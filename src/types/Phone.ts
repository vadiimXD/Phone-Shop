export type Phone = {
    name:string
    type: string
    damages: string
    image: string
    description: string
    production: string
    price: string
    buyingList: string[]
    owner: string
}